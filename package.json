{
  "name": "cv-haegele-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "smoke": "node scripts/smoke-test.mjs",
    "db:migrate:local": "wrangler d1 execute recruiting-haegele-dev --local --file=src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute recruiting-haegele-dev --remote --file=src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260418.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.86.0"
  },
  "dependencies": {
    "@cloudflare/ai-chat": "^0.6.2",
    "agents": "^0.12.3",
    "ai": "^6.0.176",
    "workers-ai-provider": "^3.1.14"
  }
}
